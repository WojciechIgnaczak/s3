microsoft cloud : Azure, DevOps (podobne do github i gitlab), Entra(zarządzanie zasobami ludzkimi, weryfikacja, autentykacja, wcześniej Active directory), GitHub, Microsoft 365 Admin, Visual Studio Code

Usługi azurowe z poziomu portalu: App Services(uruchamia coś z dockera), Function App(uruchamia tylko konkrenty skrypt), Azure Cosmos DB, Load Balancer, Storage accounts, Virtual networks, Monitor, Entra, Advisor, Defender Cloud


cechy chmury:

- szybki dostęp do sieci

- dostęp do zasobów na żądanie

- minus: transfer do jest za darmo a wyciągnięcie kosztuje

- gatewaya, walidacje

- storage

- regionowość, multizone

- pula zasobów (tworzenie wirtualnych maszyn, skalowalność natywnie, elastyczność)

- wiemy jakie są koszta


Azure Cloud Shell - zarządzanie chmurą z poziomu wierszu poleceń


Subskrybcja

Role : najlepiej jest mieć najmniej uprawnień



Resource group (grupy zasobów) - pokoje gdzie będą zasoby

utworzenie zasobów VM, np dostęp ograniczany siciowo

Resource JSON (skrypt opisujący powstanie i przypisanie resourse) - można wrzucić to na git i tworzyć zasób na podstawie tego kodu

All services -> all - pokazanie ile jest zasobów


Tworzenie VM

Compute infrastructure/Virtual machines

Create->Virtual Machine-> wybieranie subskrypcji, grupy, regionu-> Ubuntu->dostępność -> zone options (self selected), zone 1-> security type Trusyed launch-> Image (ubuntu server 24.04 LTS - x64 Gen2)-> VM architecture x64 -> size Standard D2 -> authentication type password -> inbound -> http -> os disk 30GB-> type Standard-> key managment Customer-managed key (ale wybieramy Platform) -> przepięcie na virtual network, do VM-> allocate using IP addres pool tik-> nazwy itd-> Create-> wracamy do VM a tak naprawde to tworzymy od nowa


do VM można się łączyć przez Bastion

Microsoft Learn