version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: dockerfile.python

    expose:
      - "8000"

  nginx:
    build:
      context: .
      dockerfile: dockerfile.nginx
    ports:
      - "80:80"
    depends_on:
      - web
