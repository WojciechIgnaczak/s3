version: "3.8"
services:
  app:
    build: 
      context: ./app
      dockerfile: ./Dockerfile.dev
    ports:
      - "8080:5000"
    depends_on:
      - redis
    volumes:
      - ./app:/usr/src/app

  redis:
    image: "redis:alpine"
    ports:
      - "6379:6379"